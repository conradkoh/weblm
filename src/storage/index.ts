/**
 * Storage module — IndexedDB persistence layer.
 *
 * Responsibilities:
 * - Model weight caching in IndexedDB
 * - Storage quota detection and management
 * - "Clear Model" utility for disk cleanup
 * - Offline-readiness checks
 */

export {};
